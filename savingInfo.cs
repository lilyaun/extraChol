using System.Collections;
using System.Collections.Generic;
using UnityEngine;
// i put the doublw slash in the code because it wasn't finished and was causing an error -cass
public class savingInfo
{
    public static void SaveAllInfo()
    {
 //PlayerPrefs.GetInt("PLAYERNAME", gameInfo.PlayerLevel);
    }
}